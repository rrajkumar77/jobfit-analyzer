# Secrets — never commit these
.env
*.env
!.env.example

# Candidate/HM documents — these should never live in git.
# (This is what leaked a real candidate's name previously — see masking.py docstring.)
HM_Summary*.docx
HM_Summary*.pdf
*_Summary_*.docx

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Streamlit
.streamlit/secrets.toml
